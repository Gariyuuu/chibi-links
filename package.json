{
  "name": "chibi-links",
  "version": "1.0.0",
  "private": true,
  "description": "single-file serverless url shortener — hash-fragment routing, localStorage persistence",
  "scripts": {
    "test": "node test/e2e.test.js"
  },
  "devDependencies": {
    "playwright": "^1.45.0"
  }
}
